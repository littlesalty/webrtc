{
  "name": "webrtc",
  "version": "0.0.1",
  "description": "webrtc test repo",
  "main": "src/index.ts",
  "scripts": {
    "start:windows": "npx ts-node src\\index.ts",
    "dev:windows": "npx nodemon --watch 'src\\**\\*.ts' && npm run start:windows",
    "start": "npm run ts-node src/index.ts",
    "dev": "npm run nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/index.ts"
  },
  "keywords": [
    "webrtc"
  ],
  "author": "littlesalty",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.13",
    "nodemon": "^2.0.13",
    "ts-node": "^10.2.1",
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^4.2.0"
  }
}